<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        id = "com.vrindi.restaurantwave"
        version = "0.0.2"
 >


  <name>Jashn E Tawa</name>

  <description>
    Jashn E Tawa | Online Food Ordering | Indian Restaurant Ahmedabad
  </description>

  <author href="http://vrindi.com" email="support@vrindi.com">
    Vrindi Team
  </author>

  <gap:config-file platform="ios" parent="CFBundleShortVersionString">
    <string>120</string>
  </gap:config-file>


  <feature name="http://api.phonegap.com/1.0/device" />

  <feature name="SplashScreen">
    <param name="ios-package" value="CDVSplashScreen" />
  </feature>


  <preference name="permissions" value="none"/>

  <gap:config-file platform="ios" parent="UIStatusBarHidden">
    <true/>
  </gap:config-file>
  <gap:config-file platform="ios" parent="UIViewControllerBasedStatusBarAppearance">
    <false/>
  </gap:config-file>

  <gap:plugin name="org.apache.cordova.splashscreen" source="npm" />
  <!-- Customize your app and platform with the preference element. -->
  <preference name="phonegap-version" value="3.7.0" />
  <preference name="splash-screen-duration" value="10000" />
  <!-- all: current version of PhoneGap -->
  <preference name="orientation" value="default" />
  <!-- all: default means both landscape and portrait are enabled -->
  <preference name="target-device" value="handset"  />
  <!-- all: possible values handset, tablet, or universal -->
  <preference name="fullscreen" value="true" />
  <!-- all: hides the status bar at the top of the screen -->
  <preference name="webviewbounce" value="true" />
  <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
  <preference name="prerendered-icon" value="true" />
  <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
  <preference name="stay-in-webview" value="false" />
  <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
  <preference name="ios-statusbarstyle" value="black-opaque" />
  <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
  <preference name="detect-data-types" value="true" />
  <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
  <preference name="exit-on-suspend" value="false" />
  <!-- ios: if set to true, app will terminate when home button is pressed -->
  <preference name="show-splash-screen-spinner" value="true" />
  <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
  <preference name="auto-hide-splash-screen" value="true" />
  <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
  <preference name="disable-cursor" value="false" />
  <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
  <preference name="android-minSdkVersion" value="7" />
  <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
  <preference name="android-installLocation" value="auto" />
  <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->



  <!-- Define app splash screen for each platform. -->


  <!-- Icon and splash screen for IOS platform.  -->
  <icon src="images/Icon_57x57.png" gap:platform="ios" width="57"  height="57"  />
  <icon src="images/Icon_72x72.png" gap:platform="ios" width="72"  height="72"  />
  <icon src="images/Icon_120x120.png" gap:platform="ios" width="120"  height="120"  />

  <gap:splash src="images/SplashScreen_320x480.png" gap:platform="ios" width="320"  height="480"  />
  <gap:splash src="images/SplashScreen_640x960.png" gap:platform="ios" width="640"  height="960"  />
  <gap:splash src="images/SplashScreen_768x1024.png" gap:platform="ios" width="768"  height="1024" />
  <gap:splash src="images/SplashScreen_1024x768.png" gap:platform="ios" width="1024" height="768"  />
  <gap:splash src="Images/SplashScreen_640x1136.png" gap:platform="ios" width="640" height="1136" />

  <access origin="*" />

</widget>
